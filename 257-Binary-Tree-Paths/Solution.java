public class Solution {
	public List<String> binaryTreePaths(TreeNode root) {
		List<String> result;
		Stack<TreeNode> s = new Stack<>();
		s.push(root);
		while(!s.empty()){
			TreeNode cur = s.peek();
			if(cur.left != null) {
				
			}
			
			

			
		}		
		
		return result;	
	}
}	
